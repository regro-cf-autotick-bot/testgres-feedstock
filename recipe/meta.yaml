{% set name = "testgres" %}
{% set version = "1.10.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/postgrespro/testgres/archive/refs/tags/{{ version }}.tar.gz
  sha256: 8ddaacb6769e9416d2e4d9687c30496a85b1858e44e4cddfbc189134c8b64cd1


build:
  number: 0
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python >=3.6
    - pip
    - setuptools
  run:
    - python >=3.6
    - pg8000
    - port-for >=0.4
    - six >=1.9.0
    - psutil
    - packaging

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - testgres
    - testgres.operations

about:
  home: https://github.com/postgrespro/testgres
  license: PostgreSQL
  license_family: MIT
  license_file: LICENSE
  summary: 'Testing utility for PostgreSQL and its extensions.'

extra:
  recipe-maintainers:
    - demonolock
